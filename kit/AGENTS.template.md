# <项目名> 核心索引（自动加载）

> **A 内核（核心索引层）**：只存检索键与跨域硬约束。按域避坑细则在 `.pi/rules/*.md`，memory-gate 四闸门（域规则 / 大文件附注 / compaction 恢复 / sleep 节律）按代码域自动注入（tool_call 拦截）。
> 范式 spec：`README.md`；工作流：`.pi/APPEND_SYSTEM.md`；设计决定：`Blueprint.md`（可选）。

## 文档分层

| 文件 | 本质 | 加载时机 |
|------|------|---------|
| `Blueprint.md` | 设计决定（架构 + Mermaid 图；可选） | 设计/架构/改契约时读 |
| `plan.md` | 计划 schema（唯一来源） | 生成计划时套用 |
| `README.md` | 范式 spec（A 内核 §5 / 双模注入 §2.3 / 技能契约 §2.4（液→固）/ 符号阀 §3） | 范式/流程问题时读 |
| `AGENTS.md`（本文） | 实现事实核心索引 | 每次会话自动加载 |
| `.pi/rules/*.md` | 按域避坑细则（储层） | memory-gate 自动注入 |
| `.pi/prompts/` | /plan 与 /sleep 命令模板 | 手动触发工作流 |

数据流：`Blueprint --plan--> 计划 --执行--> 代码`；经验回流 `.pi/rules/<域>.md` / `Blueprint.md`（设计决定需人批准）。

## 规则域映射

<域A>←`<路径前缀>` · <域B>←`<路径前缀>`（对应 `.pi/rules/<域>.md`；与 memory-gate ROUTES 同步）

## 环境信息

- **语言/构建/测试**：<语言、构建命令、测试命令及注意事项>
- **配置**：<配置文件位置与硬约束>
- **命名**：<项目命名惯例>

## 模块路径索引

| 层/模块 | 路径 | 职责（一句话） |
|----|------|------|
| ... | ... | ... |

## 关键类型/接口索引

| 类型 | 路径 |
|------|------|
| ... | ... |

## 机械守护（跑 /check 验证）

R5 幽灵路径 / R6 计划对碰 / R7 探索纯净（模板自带）；R1–R4 按项目沉淀启用（配置 `bcp/bcp.toml`，实现 `bcp/check.py`，改规则同步两处）。

## 跨域硬约束

- **文档先行**：模块结构/类型设计/接口契约/数据流变更必须先更新设计决定文档；纯内部实现无需。冲突仲裁：设计决定与代码冲突以设计决定为准；实现事实与代码冲突以代码为准；命名以代码为准。
- **错误处理**：<项目错误处理惯例>
- **测试惯例**：<临时文件清理 / 并行隔离 / 断言方式>
- <项目专属硬约束>
